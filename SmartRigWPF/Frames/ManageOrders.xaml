<UserControl x:Class="SmartRigWPF.Frames.ManageOrders"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Manage Orders"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#2c3e50"
                   Margin="0,0,0,20"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="View Details"
                    Width="110"
                    Height="35"
                    Background="#3498db"
                    Foreground="White"
                    BorderThickness="0"
                    Margin="0,0,10,0"
                    Cursor="Hand"/>
            <Button Content="Mark as Paid"
                    Width="110"
                    Height="35"
                    Background="#27ae60"
                    Foreground="White"
                    BorderThickness="0"
                    Margin="0,0,10,0"
                    Cursor="Hand"/>
            <Button Content="Delete Order"
                    Width="110"
                    Height="35"
                    Background="#e74c3c"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid Grid.Row="2"
                  Name="OrdersDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#f8f9fa"
                  Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Cart ID" Binding="{Binding CartId}" Width="80"/>
                <DataGridTextColumn Header="User ID" Binding="{Binding UserId}" Width="80"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding IsPayed}" Width="100"/>
                <DataGridTextColumn Header="Total Items" Binding="{Binding ItemCount}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>