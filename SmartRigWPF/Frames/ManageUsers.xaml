<UserControl x:Class="SmartRigWPF.Frames.ManageUsers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Title -->
		<TextBlock Grid.Row="0"
                   Text="Manage Users"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#2c3e50"
                   Margin="0,0,0,20"/>

		<!-- Action Buttons -->
		<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
			<Button Content="Add User"
                    Width="120"
                    Height="35"
                    Background="#27ae60"
                    Foreground="White"
                    BorderThickness="0"
                    Margin="0,0,10,0"
                    Cursor="Hand"/>

			<Button Content="Edit User"
                    Width="120"
                    Height="35"
                    Background="#3498db"
                    Foreground="White"
                    BorderThickness="0"
                    Margin="0,0,10,0"
                    Cursor="Hand"/>

			<Button Content="Delete User"
                    Width="120"
                    Height="35"
                    Background="#e74c3c"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"/>
		</StackPanel>

		<!-- Data Grid -->
		<DataGrid Grid.Row="2"
                  Name="UsersDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#f8f9fa"
                  Background="White">
			<DataGrid.Columns>
				<DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="60"/>
				<DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
				<DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
				<DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="120"/>
				<DataGridTextColumn Header="Created" Binding="{Binding CreatedDate}" Width="140"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
