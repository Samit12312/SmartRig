<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SmartRigWPF"
    x:Class="SmartRigWPF.Frames.ManageUsers"
    Background="White"
    Width="730" Height="540">

    <Grid>
        
        <TextBlock Text="Manage Users"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#2c3e50"
                   Margin="20,20,0,0"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"/>

        <StackPanel Orientation="Horizontal"
            Margin="20,65,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top">
            <Button Content="Edit User" Width="120" Height="35"
    Background="#3498db" Foreground="White" BorderThickness="0" Cursor="Hand"
    Click="EditBtn_Click"/>
        </StackPanel>

        <ListView x:Name="listView"
          Margin="20,120,20,30"
          BorderBrush="#dee2e6"
          BorderThickness="1"
                  Height="200"
          ScrollViewer.VerticalScrollBarVisibility="Auto">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="12"
                    BorderBrush="#ecf0f1"
                    BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>

                                <TextBlock Text="{Binding UserName}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock FontSize="12" Foreground="#7f8c8d" Margin="0,2,0,0">
                            <Run Text="User ID: "/>
                            <Run Text="{Binding UserId}"/>
                                </TextBlock>

                                <WrapPanel Margin="0,6,0,0">
                                    <TextBlock Text="Email: " Width="120"/>
                                    <TextBlock Text="{Binding UserEmail}"/>
                                </WrapPanel>

                                <WrapPanel>
                                    <TextBlock Text="Phone: " Width="120"/>
                                    <TextBlock Text="{Binding UserPhoneNumber}"/>
                                </WrapPanel>

                                <WrapPanel>
                                    <TextBlock Text="Address: " Width="120"/>
                                    <TextBlock Text="{Binding UserAddress}"/>
                                </WrapPanel>

                                <WrapPanel>
                                    <TextBlock Text="City ID: " Width="120"/>
                                    <TextBlock Text="{Binding CityId}"/>
                                </WrapPanel>

                                <WrapPanel Margin="0,4,0,0">
                                    <TextBlock Text="Manager: " Width="120"/>
                                    <TextBlock Text="{Binding Manager}" FontWeight="Bold" Foreground="#3498db"/>
                                </WrapPanel>

                            </StackPanel>

                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>