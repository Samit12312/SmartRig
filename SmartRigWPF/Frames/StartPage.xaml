<UserControl x:Class="SmartRigWPF.Frames.StartPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="800"
             Background="#F4F6F8">

    <!-- 1️⃣ Resources -->
    <UserControl.Resources>
        <!-- Drop shadow effect -->
        <DropShadowEffect x:Key="DropShadowEffect"
                          Color="Black"
                          BlurRadius="10"
                          Opacity="0.2"
                          ShadowDepth="3"/>

        <!-- Dashboard button style -->
        <Style x:Key="DashboardButton" TargetType="Button">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#6460D5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="buttonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                SnapsToDevicePixels="True"
                                Effect="{StaticResource DropShadowEffect}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="buttonBorder" Property="Background" Value="#5047C5"/>
                                <Setter TargetName="buttonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="12" ShadowDepth="4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="buttonBorder" Property="Background" Value="#3F3AB5"/>
                                <Setter TargetName="buttonBorder" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Image Source="/Images/HomePage.jpg" Stretch="UniformToFill" Opacity="0.25"/>
        <Grid Background="#66000000"/>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Dashboard" FontSize="28" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Grid.Row="0" Grid.Column="0" Content="Manage Users" Style="{StaticResource DashboardButton}" Click="Button_Click_4"/>
                <Button Grid.Row="0" Grid.Column="1" Content="Manage Computers" Style="{StaticResource DashboardButton}" Click="Button_Click_3"/>
                <Button Grid.Row="0" Grid.Column="2" Content="Computer Components" Style="{StaticResource DashboardButton}" Click="Button_Click_2"/>
                <Button Grid.Row="1" Grid.Column="0" Content="Manage Orders" Style="{StaticResource DashboardButton}" Click="Button_Click_1"/>
                <Button Grid.Row="1" Grid.Column="1" Content="View Reports" Style="{StaticResource DashboardButton}" Click="Button_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
